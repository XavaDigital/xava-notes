{
  "name": "xava-notes-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute xava-notes --file schema.sql",
    "db:init:remote": "wrangler d1 execute xava-notes --remote --file schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
